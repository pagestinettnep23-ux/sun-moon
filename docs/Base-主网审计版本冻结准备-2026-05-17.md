# Base 主网审计版本冻结准备 - 2026-05-17

本文档用于把当前 SUN/MOON Base 主网候选版本整理成“可交给审计方的固定版本”。它不是主网部署批准，也不包含私钥。

当前仍然：

```text
不部署 Base 主网
不广播交易
不接真实资金
不索要私钥
```

## 1. 当前 git 状态

已检查：

```text
git 仓库存在
当前分支：master
当前 HEAD 含义：可能是 rc2 之后的审计交付文档同步提交
当前审计代码 commit：6b136193590bc981eb4689b822b8c921206d9d37
当前审计代码 tag：audit-sun-moon-base-contracts-2026-05-17-rc2
前一个审计候选 tag：audit-sun-moon-base-contracts-2026-05-17-rc1
当前未纳入合约审计范围的未跟踪目录：frontend/
```

这意味着当前已经可以把审计代码版本写成固定的 commit hash 和 tag。`rc2` 是 Slither 免费工具预审后修复并补充报告的当前建议交审代码版本；后续文档同步提交不改变 `rc2` 指向的合约代码。

## 2. 已创建的冻结版本

已创建版本：

```text
rc1 commit: f8afc446e1787f6ed60fefa7ebb318ea17840882
rc1 tag: audit-sun-moon-base-contracts-2026-05-17-rc1
rc1 含义：初始合约审计包冻结版本

rc2 commit: 6b136193590bc981eb4689b822b8c921206d9d37
rc2 tag: audit-sun-moon-base-contracts-2026-05-17-rc2
rc2 含义：Slither 免费工具预审修复和报告补充后的当前建议交审版本
```

命名含义：

```text
audit = 给审计用
sun-moon-base = SUN/MOON Base 方案
2026-05-17 = 冻结日期
rc = release candidate，审计候选版本
```

如果审计后修改代码，应创建新的 commit 和新的 tag，例如：

```text
audit-sun-moon-base-2026-05-17-rc2
audit-sun-moon-base-2026-05-18-rc1
```

## 3. 建议纳入审计 commit 的内容

核心工程文件：

```text
.gitignore
.gitmodules
.env.example
README.md
foundry.toml
foundry.lock
remappings.txt
```

合约：

```text
contracts/
```

脚本：

```text
script/
```

测试：

```text
test/
```

依赖：

```text
lib/forge-std              作为 submodule gitlink 纳入
lib/openzeppelin-contracts 作为 submodule gitlink 纳入
lib/prb-math               作为 submodule gitlink 纳入

lib/v4-core/src            作为 vendored source 纳入
lib/v4-core/licenses       作为 vendored license 纳入
lib/v4-core/README.md      作为 vendored 说明纳入

lib/v4-periphery/src       作为 vendored source 纳入
lib/v4-periphery/LICENSE   作为 vendored license 纳入
lib/v4-periphery/README.md 作为 vendored 说明纳入

lib/permit2/src            作为 vendored source 纳入
lib/permit2/LICENSE        作为 vendored license 纳入
lib/permit2/README.md      作为 vendored 说明纳入

lib/solmate/src            作为 vendored source 纳入
lib/solmate/LICENSE        作为 vendored license 纳入
lib/solmate/README.md      作为 vendored 说明纳入
```

文档：

```text
docs/
```

前端源码可以纳入仓库，但智能合约审计主范围仍以合约和脚本为主：

```text
frontend/app/
frontend/index.html
frontend/package.json
frontend/package-lock.json
frontend/next.config.ts
frontend/postcss.config.mjs
frontend/static-server.mjs
frontend/tsconfig.json
frontend/README_OPEN.txt
```

## 4. 不建议纳入审计 commit 的内容

这些不应进入审计版本：

```text
.env
.env.*
out/
cache/
broadcast/
lib/
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/tsconfig.tsbuildinfo
frontend/*.backup-*.html
frontend/*.before-*.html
frontend-backup-before-pencil-*/
*.log
```

说明：

```text
大部分 lib/ 内容不纳入；但 v4-core、v4-periphery、permit2、solmate 的必要 src/license/readme 需要纳入，否则审计方无法复现编译
out/、cache/、broadcast/ 是本地生成物
frontend 备份 HTML 和 tsbuildinfo 是本地过程文件
.env 可能包含密钥或 RPC，不允许进入版本库
```

## 5. 已加的误提交防护

`.gitignore` 已补充：

```text
.slither-home/
frontend/tsconfig.tsbuildinfo
frontend/*.backup-*.html
frontend/*.before-*.html
frontend-backup-before-pencil-*/
```

这些改动只影响 git 收录范围，不影响合约逻辑，不影响测试。

## 5.1 当前 submodule 记录

当前主仓库 index 已记录 3 个依赖 submodule：

```text
lib/forge-std              620536fa5277db4e3fd46772d5cbc1ea0696fb43
lib/openzeppelin-contracts 5fd1781b1454fd1ef8e722282f86f9293cacf256
lib/prb-math               b51e8631ed28d3cc917c491dd47cd6c3ff652edc
```

提示：在 Codex 沙盒用户下运行 `git submodule status` 会遇到 Windows 文件所有者不同的 safe.directory 提示。这不是合约测试失败，也不是链上问题。创建审计 commit 前，owner 可以在自己的终端再复核一次 submodule 状态。

另外 4 个编译所需依赖当前不是主仓库 submodule，已按 vendored source 范围处理：

```text
lib/v4-core
lib/v4-periphery
lib/permit2
lib/solmate
```

## 6. 创建审计 commit 前复核记录

- [x] `git status --short` 中没有 `.env`。
- [x] `git status --short` 中没有 `out/`。
- [x] `git status --short` 中没有 `cache/`。
- [x] `git status --short` 中没有 `broadcast/`。
- [x] `git status --short` 中没有 `node_modules/`。
- [x] `git status --short` 中没有 `frontend-backup-before-pencil-*`。
- [x] `git status --short` 中没有前端备份 HTML。
- [x] `forge fmt` 已跑过。
- [x] `forge test --threads 1 --isolate` 已通过。
- [x] 审计输入包已经更新。
- [x] 上线前人工复核清单已经更新。

说明：

```text
当前 git status 只剩 frontend/ 未跟踪目录，这是因为本轮选择“合约审计范围”，没有把前端纳入审计 commit。
```

## 7. 已执行的本地版本记录

本轮选择冻结合约审计范围，不纳入前端：

```text
rc1 commit message: prepare SUN MOON Base mainnet contract audit package
rc1 tag: audit-sun-moon-base-contracts-2026-05-17-rc1

rc2 commit message: address Slither pre-audit findings
rc2 tag: audit-sun-moon-base-contracts-2026-05-17-rc2
```

说明：

```text
rc1 是免费工具预审前的初始审计包。
rc2 是当前建议交审版本。
后续如果审计发现问题并修复，应继续创建 rc3 或改用新的日期标签，不覆盖 rc1/rc2。
```

## 8. commit 后必须记录

创建 commit 和 tag 后，把实际输出补到这里：

```text
AUDIT_COMMIT_HASH=6b136193590bc981eb4689b822b8c921206d9d37
AUDIT_TAG=audit-sun-moon-base-contracts-2026-05-17-rc2
AUDIT_PREVIOUS_TAG=audit-sun-moon-base-contracts-2026-05-17-rc1
AUDIT_TEST_COMMAND=forge test --threads 1 --isolate
AUDIT_TEST_RESULT_AT_RC2=296 passed, 0 failed
LOCAL_STAGE_A_TEST_RESULT=307 passed, 0 failed
AUDIT_SLITHER_REPORT=docs/slither-report-2026-05-17.json
AUDIT_SLITHER_HUMAN_REPORT=docs/Base-主网免费工具审查报告-2026-05-17.md
AUDIT_FREE_REVIEW_STAGE_A_REPORT=docs/Base-主网免费审计替代流程-阶段A-2026-05-17.md
```

然后同步更新：

```text
docs/Base-主网审计输入包-2026-05-17.md
docs/Base-主网上线前人工复核清单-2026-05-17.md
docs/安全检查清单.md
README.md
```

## 9. 当前建议

当前已经冻结合约审计范围，并建议使用 `rc2` 交给审计方：

```text
包含：contracts、script、test、docs、README 和 Foundry 配置
包含：编译所需依赖的 submodule 记录和 vendored source
暂不包含：frontend
当前审计代码 commit：6b136193590bc981eb4689b822b8c921206d9d37
当前审计代码 tag：audit-sun-moon-base-contracts-2026-05-17-rc2
```

原因：

```text
当前审计重点是智能合约和主网脚本
frontend 里有多份历史备份和预览文件，最好后续单独整理
合约审计版本越干净，审计方越容易聚焦
```

当前已经执行 commit 并创建 tag。下一步不是主网部署，而是把 `rc2` 材料交给外部审计或安全 review。
