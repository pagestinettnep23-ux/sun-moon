# Base 主网部署安全整理 - 旧 Hook 禁用说明 - 2026-05-20

本文记录本次只做部署安全整理，不改核心合约逻辑。

```text
不执行 Base 主网交易
不使用真实资金
不要求或记录私钥、助记词、恢复词
不使用 --private-key
不广播
不改 SunCurve / MoonCurve / Token / MoonCurveMath 核心逻辑
```

## 1. 当前结论

rc4 / Base mainnet 上线路径只允许使用新版统一 Hook：

```text
BaseSunMoonUsdcFeeV4Hook
```

旧版 `BaseMoonAmmFeeV4Hook`、旧 salt 脚本、旧 Base Sepolia Hook 部署 / 绑定 / `MOON/USDC` 小额演练脚本均为：

```text
deprecated / legacy
旧方案，不用于 rc4/mainnet
```

## 2. 当前主网上线前应使用的脚本

只读 / dry-run 路径：

```text
script/PrepareBaseMainnetCoreDeployDryRun.s.sol
script/ComputeBaseMainnetSunMoonUsdcHookSalt.s.sol
script/ComputeBaseSunMoonUsdcPoolIds.s.sol
script/PrepareBaseMainnetSunMoonUsdcForkDryRun.s.sol
```

本地 CREATE2 预演可参考：

```text
script/RehearseBaseSunMoonUsdcFeeV4Hook.s.sol
```

## 3. 已标记为 legacy 的高风险旧入口

这些脚本仍保留为历史测试网资料，但已经在源码和文档里标记为旧方案：

```text
script/FindBaseMoonAmmFeeV4HookSalt.s.sol
script/RehearseCreate2HookDeployer.s.sol
script/CheckBaseSepoliaDeploymentParams.s.sol
script/PrepareBaseSepoliaHookDeploy.s.sol
script/PrepareBaseSepoliaHookBinding.s.sol
script/PrepareBaseSepoliaControlledMoonPool.s.sol
script/PrepareBaseSepoliaControlledMoonPoolInitialize.s.sol
script/PrepareBaseSepoliaTinyMoonUsdcRehearsal.s.sol
script/PrecheckBaseSepoliaTinyMoonUsdcQuote.s.sol
script/PrepareBaseSepoliaTinyMoonUsdcBroadcast.s.sol
```

说明：

```text
这些旧脚本不是 rc4/mainnet 路径。
不要用旧 BaseMoonAmmFeeV4Hook salt 计算主网 Hook。
不要用旧 PrepareBaseSepoliaHookDeploy 当主网部署模板。
不要用旧 TinyMoonUsdc 演练脚本推导当前主网流程。
```

## 4. 已整理的文档入口

```text
README.md
script/README.md
docs/README.md
docs/Base-主网上线前人工复核清单-2026-05-17.md
docs/Base-主网正式合约地址与CREATE2参数草案-2026-05-17.md
docs/Base-主网审计输入包-2026-05-17.md
docs/Base-主网审计候选版本与输入包索引-2026-05-19.md
docs/Base-主网rc4审计包核对清单-2026-05-19.md
```

这些文档现在会先提示：

```text
rc4/mainnet 只使用 BaseSunMoonUsdcFeeV4Hook
旧 BaseMoonAmmFeeV4Hook 路径是 deprecated / legacy
旧方案不用于 rc4/mainnet
```

## 5. 验证结果

格式化：

```text
forge fmt
passed
```

主网 dry-run / 部署预检小范围回归：

```text
forge test --match-path "test/hooks/base/BaseMainnet*.t.sol" --threads 1
45 passed, 0 failed, 0 skipped
```

旧 Base Sepolia 脚本编译与历史回归：

```text
forge test --match-path "test/hooks/base/BaseSepolia*.t.sol" --threads 1
74 passed, 0 failed, 0 skipped
```

新版 Hook / poolId / CREATE2 回归：

```text
forge test --match-path "test/hooks/base/BaseSunMoonUsdc*.t.sol" --threads 1
28 passed, 0 failed, 0 skipped
```

旧本地 CREATE2 预演脚本仍可作为历史测试通过：

```text
forge test --match-path "test/hooks/base/Create2HookDeployerRehearsal.t.sol" --threads 1
1 passed, 0 failed, 0 skipped
```

Base Sepolia 旧参数预检库回归：

```text
forge test --match-path "test/hooks/base/BaseDeploymentPreflight.t.sol" --threads 1
6 passed, 0 failed, 0 skipped
```

当前主网四个脚本中检查旧 Hook 关键字：

```text
rg -n "BaseMoonAmmFeeV4Hook|FindBaseMoonAmmFeeV4HookSalt|PrepareBaseSepoliaHookDeploy|TinyMoonUsdc" \
  script/PrepareBaseMainnetCoreDeployDryRun.s.sol \
  script/ComputeBaseMainnetSunMoonUsdcHookSalt.s.sol \
  script/ComputeBaseSunMoonUsdcPoolIds.s.sol \
  script/PrepareBaseMainnetSunMoonUsdcForkDryRun.s.sol
```

结果：

```text
no matches
```

## 6. 上线前仍需保留的硬闸门

```text
SunCurve.usdt() == USDC_TOKEN
BaseSunMoonUsdcFeeV4Hook.usdc() == USDC_TOKEN
SunCurve.moonAMM() == Hook
```

如果未来任何脚本、文档或命令默认指向 `BaseMoonAmmFeeV4Hook`，立即停止，先回到本清单复核。
