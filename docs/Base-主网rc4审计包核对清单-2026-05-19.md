# Base 主网 rc4 审计包核对清单 - 2026-05-19

本文用于核对 `rc4` 审计候选版本包含哪些内容，方便你或审计人员按 tag 查看。

注意：本文是在 `rc4` tag 创建后生成的辅助核对清单，不改变 `rc4` tag 内容。

```text
不执行 Base 主网交易
不使用真实资金
不要求或记录私钥、助记词、恢复词
不使用 --private-key
不广播
不改 tag
```

## 1. rc4 标签信息

```text
TAG=audit-sun-moon-base-contracts-2026-05-19-rc4
COMMIT=8d7ccf3
COMMIT_MESSAGE=Prepare rc4 audit candidate fixes
```

状态：

```text
本地 tag 已创建。
frontend/ 未进入该 tag。
```

## 2. 审计人员如何查看 rc4

只读查看 tag 指向的提交：

```powershell
git show --stat audit-sun-moon-base-contracts-2026-05-19-rc4
```

只读查看 tag 包含的文件名：

```powershell
git show --name-only --format="%h %s" audit-sun-moon-base-contracts-2026-05-19-rc4
```

只读确认 tag 指向的 commit：

```powershell
git rev-parse --short audit-sun-moon-base-contracts-2026-05-19-rc4
```

预期输出：

```text
8d7ccf3
```

## 3. rc4 包含的合约文件

```text
contracts/SunCurve.sol
contracts/MoonCurve.sol
contracts/hooks/base/BaseSunMoonUsdcFeeV4Hook.sol
```

核对目的：

```text
确认 lastMintBlock DoS 修复已经进入 rc4。
确认空 hookData 兼容性修复已经进入 rc4。
```

## 4. rc4 包含的测试文件

```text
test/SunCurve.t.sol
test/MoonCurve.t.sol
test/hooks/base/BaseSunMoonUsdcFeeV4Hook.t.sol
```

核对目的：

```text
确认 lastMintBlock DoS 修复有对应测试。
确认空 hookData swap 成功、min=0 失败、min 过高失败、极小额 fee=0 失败都有对应测试。
```

## 5. rc4 包含的主网和审计文档

```text
README.md
script/README.md
docs/Base-主网rc4提交前文件清单草案-2026-05-19.md
docs/Base-主网上线前人工复核清单-2026-05-17.md
docs/Base-主网审计修复记录-2026-05-19.md
docs/Base-主网审计候选版本与输入包索引-2026-05-19.md
docs/Base-主网正式合约地址与CREATE2参数草案-2026-05-17.md
docs/安全检查清单.md
```

核对目的：

```text
确认新的 HOOK_SALT、PREDICTED_HOOK、poolId、Base mainnet fork dry-run 结果已经记录。
确认上线前人工硬闸门已经记录。
确认审计输入包范围已经记录。
```

## 6. rc4 包含的 Base Sepolia 背景资料

```text
docs/Base-Sepolia-3天只读观察清单-2026-05-19.md
docs/Base-Sepolia-3天只读观察记录-2026-05-19.md
```

说明：

```text
这些只是测试网观察背景资料。
它们不是 Base 主网部署凭证。
```

## 7. rc4 不包含的内容

```text
frontend/
```

当前工作区里仍能看到前端未跟踪文件，例如：

```text
frontend/app/page.tsx
frontend/package.json
frontend/static-server.mjs
```

说明：

```text
这是有意排除。
rc4 是合约审计候选版本，不是前端发布版本。
```

## 8. rc4 已记录的测试凭证

```text
forge test --match-path test\hooks\base\BaseSunMoonUsdcFeeV4Hook.t.sol
18 passed, 0 failed

Base Hook / CREATE2 / 部署预检小范围回归
82 passed, 0 failed

forge test --threads 1 --isolate
366 tests passed
0 failed
0 skipped
```

说明：

```text
这些是本地 Foundry 测试结果。
没有广播，没有执行 Base 主网交易，没有使用真实资金，没有使用或记录私钥。
```

## 9. rc4 上线前硬闸门

上线前必须再次确认：

```text
SunCurve.usdt() == USDC_TOKEN
BaseSunMoonUsdcFeeV4Hook.usdc() == USDC_TOKEN
SunCurve.moonAMM() == Hook
```

部署路径还必须确认：

```text
rc4/mainnet 只使用 BaseSunMoonUsdcFeeV4Hook
旧 BaseMoonAmmFeeV4Hook / FindBaseMoonAmmFeeV4HookSalt / PrepareBaseSepoliaHookDeploy / TinyMoonUsdc 脚本均为 legacy
旧方案不用于 rc4/mainnet
```

还必须再次确认：

```text
重新计算 HOOK_SALT 和 PREDICTED_HOOK
重新计算 SUN_USDC_POOL_ID 和 MOON_USDC_POOL_ID
重新跑 Base mainnet fork 总 dry-run
确认 PREDICTED_HOOK 低 14 位权限 bit 等于 204
确认没有 --broadcast
确认没有 --private-key
确认没有使用或记录私钥、助记词、恢复词
```

## 10. 当前结论

```text
rc4 tag 已创建。
rc4 tag 指向 8d7ccf3。
rc4 包含本轮合约修复、测试和审计文档。
rc4 不包含 frontend/。
rc4 不是主网上线批准。
```

下一步建议：

```text
只读复核这份 rc4 审计包核对清单。
确认无误后，再决定是否把 tag 和 commit 推送到远程仓库，或交给审计人员。
```
